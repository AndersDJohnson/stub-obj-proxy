{
  "name": "stub-obj-proxy",
  "version": "1.0.0",
  "description": "Make stubs using Proxies with deep paths, function calls, loops, etc.",
  "main": "dist/index.js",
  "license": "MIT",
  "repository": "AndersDJohnson/stub-obj-proxy",
  "scripts": {
    "build": "babel src --ignore '*.test.js,__tests__/*' -d dist",
    "test": "jest"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^21.2.1"
  }
}
